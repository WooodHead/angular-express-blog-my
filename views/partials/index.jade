h1 partials/index.jade

div(ng-repeat='post in posts')
  h2 {{post.title}}
  p {{post.text}}
  p: a(href='/readPost/{{post.id}}') More
  p: a(href='/editPost/{{post.id}}') Edit
  p: a(href='/deletePost/{{post.id}}') Delete
